<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Time Report - Tracket</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
  <div class="container py-5">
    <h2>Time Report</h2>
    <p>User: <%= userId %></p>
    <p>From: <%= fromDate %> To: <%= toDate %></p>

    <div class="row mb-4">
      <div class="col">
        <div class="card text-bg-light shadow-sm">
          <div class="card-body">
            <strong>Total Hours:</strong> <%= total %> hrs<br>
            <strong>Billable:</strong> <%= billable %> hrs<br>
            <strong>Non-Billable:</strong> <%= nonBillable %> hrs
          </div>
        </div>
      </div>
    </div>

    <table class="table table-bordered bg-white shadow-sm">
      <thead class="table-light">
        <tr>
          <th>Task</th>
          <th>Description</th>
          <th>Duration</th>
          <th>Billable</th>
          <th>Date</th>
        </tr>
      </thead>
      <tbody>
        <% logs.forEach(log => { %>
          <tr>
            <td><%= log.task?.title || 'N/A' %></td>
            <td><%= log.description %></td>
            <td><%= log.duration %> hrs</td>
            <td><%= log.billable ? 'Yes' : 'No' %></td>
            <td><%= log.createdAt.toDateString() %></td>
          </tr>
        <% }) %>
      </tbody>
    </table>

   

<h4>Export Options</h4>
<a href="/reports/export?user=<%= userId %>&from=<%= fromDate %>&to=<%= toDate %>" class="btn btn-outline-primary btn-sm">⬇️ CSV</a>
<a href="/reports/export/excel?user=<%= userId %>&from=<%= fromDate %>&to=<%= toDate %>" class="btn btn-outline-success btn-sm">⬇️ Excel</a>
<a href="/reports/export/pdf?user=<%= userId %>&from=<%= fromDate %>&to=<%= toDate %>" class="btn btn-outline-danger btn-sm">⬇️ PDF</a>


<a href="/reports/export?user=<%= userId %>&from=<%= fromDate %>&to=<%= toDate %>">Export as CSV</a><br>
<a href="/reports">Back to Filter</a>
  </div>
</body>
</html>

